# 🔧 Solución rápida: Error 404 en GitHub Pages

## El problema

Ves un error 404 al intentar acceder a tu app en GitHub Pages. Esto puede deberse a:

1. ❌ GitHub Pages no está habilitado
2. ❌ Está usando un branch incorrecto
3. ❌ El deployment está en progreso
4. ❌ Problema con dominio personalizado (CNAME)

---

## ✅ Solución (5 minutos)

### Paso 1: Verificar estado de GitHub Pages

1. Ve a tu repositorio en GitHub:
   ```
   https://github.com/apolmig/algaener
   ```

2. Navega a: **Settings → Pages** (menú lateral izquierdo)

3. Verifica el estado:
   - ✅ **"Your site is live at..."** → Todo bien, espera unos minutos
   - ⚠️ **"GitHub Pages is currently disabled"** → Necesitas habilitarlo
   - ⚠️ **"Error: Page build failed"** → Hay un problema

---

### Paso 2: Habilitar/Configurar GitHub Pages

Si Pages no está habilitado o tiene error:

**En Settings → Pages:**

1. **Source:** `Deploy from a branch`

2. **Branch:** Selecciona `claude/review-codebase-C59f4`
   - Si no aparece, intenta con `main` o la branch principal

3. **Folder:** `/ (root)`

4. Clic en **Save**

5. Espera 2-3 minutos para el deployment

---

### Paso 3: Verificar archivos críticos

Estos archivos DEBEN existir en la raíz del repositorio:

```bash
✅ index.html           # Página de entrada
✅ standalone.html      # App principal
✅ .nojekyll           # Configuración Pages
✅ manifest-standalone.json  # PWA config
✅ frontend/icon.svg   # Icono
```

Para verificar, ve a:
```
https://github.com/apolmig/algaener/tree/claude/review-codebase-C59f4
```

Y confirma que ves estos archivos.

---

### Paso 4: Probar URLs alternativas

Intenta acceder con estas URLs:

1. **URL principal:**
   ```
   https://apolmig.github.io/algaener/
   ```

2. **Standalone directo:**
   ```
   https://apolmig.github.io/algaener/standalone.html
   ```

3. **Index directo:**
   ```
   https://apolmig.github.io/algaener/index.html
   ```

4. **Con branch en URL (si Pages usa otra branch):**
   ```
   https://apolmig.github.io/algaener/main/standalone.html
   ```

---

### Paso 5: Forzar rebuild (si nada funciona)

```bash
# En tu terminal local:
cd /home/user/algaener
git checkout claude/review-codebase-C59f4
git commit --allow-empty -m "Force GitHub Pages rebuild"
git push origin claude/review-codebase-C59f4
```

Luego espera 2-3 minutos y vuelve a intentar.

---

## 🔍 Diagnosticar el problema

### Revisar el deployment log

1. Ve a: **Actions** (pestaña en GitHub)
2. Busca: **"pages build and deployment"**
3. Clic en el workflow más reciente
4. Revisa los logs para errores

### Problemas comunes y soluciones

#### Error: "404 - File not found"
**Causa:** Archivo no existe o ruta incorrecta
**Solución:** Verifica que `index.html` y `standalone.html` existen en la raíz

#### Error: "Your site is having problems building"
**Causa:** Archivo `.nojekyll` falta
**Solución:** El archivo ya está en el repo, verifica que esté en la branch correcta

#### Error: "CNAME already taken"
**Causa:** Dominio personalizado `algaener.com` tiene conflicto
**Solución temporal:**
1. Settings → Pages
2. Elimina el custom domain
3. Guarda y espera rebuild

---

## 🚀 Alternativa: Ejecutar localmente

Si GitHub Pages da problemas, puedes usar la app localmente:

```bash
# Opción 1: Doble clic en el archivo
# Ve a la carpeta algaener/
# Doble clic en standalone.html

# Opción 2: Servidor local
cd /home/user/algaener
python3 -m http.server 8000
# Abre: http://localhost:8000/standalone.html
```

---

## 📞 ¿Sigue sin funcionar?

### Información para debug

Proporciona esta info si necesitas ayuda:

1. **URL que intentas acceder:**
   ```
   [Tu URL aquí]
   ```

2. **Error exacto del navegador:**
   ```
   [Copia el error de DevTools → Console]
   ```

3. **Estado de GitHub Pages:**
   ```
   Settings → Pages → [Copia el mensaje que aparece]
   ```

4. **Branch actual:**
   ```bash
   git branch --show-current
   # Output: [pega aquí]
   ```

5. **Archivos en la raíz:**
   ```bash
   ls -la index.html standalone.html .nojekyll
   # Output: [pega aquí]
   ```

---

## ✅ Checklist de verificación

- [ ] GitHub Pages está habilitado en Settings
- [ ] Branch correcta seleccionada (claude/review-codebase-C59f4 o main)
- [ ] Folder está en / (root)
- [ ] Han pasado al menos 3 minutos desde el último cambio
- [ ] index.html existe en la raíz
- [ ] standalone.html existe en la raíz
- [ ] .nojekyll existe en la raíz
- [ ] No hay errores en Actions → Workflows
- [ ] He intentado con /standalone.html directo
- [ ] He probado en otro navegador
- [ ] He limpiado caché del navegador (Ctrl+Shift+R)

---

## 🎯 Resumen rápido

```bash
# 1. Habilita GitHub Pages
Settings → Pages → Deploy from branch → claude/review-codebase-C59f4

# 2. Espera 3 minutos

# 3. Accede a:
https://apolmig.github.io/algaener/standalone.html

# 4. Si falla, fuerza rebuild:
git commit --allow-empty -m "Rebuild"
git push

# 5. Si sigue fallando, usa local:
python3 -m http.server 8000
# http://localhost:8000/standalone.html
```

---

**¿Ya funciona?** ✅ ¡Genial! Ahora puedes compartir la URL con otros.

**¿Sigue fallando?** Abre un issue con la información de debug de arriba.
